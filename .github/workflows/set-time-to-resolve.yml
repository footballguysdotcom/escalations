# Set "Time to resolve" when an escalation issue is closed.
# Requires: In the Escalations project (footballguysdotcom, project 5), add a Number
# custom field named exactly "Time to resolve (days)" (without quotes).

name: Set time to resolve on close

on:
  issues:
    types: [closed]

jobs:
  set-time-to-resolve:
    runs-on: ubuntu-latest
    steps:
      - name: Set Time to resolve (days) on project item
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.PROJECT_TOKEN }}
          script: |
            const issue = context.payload.issue;
            const created = new Date(issue.created_at);
            const closed = new Date(issue.closed_at);
            const days = Math.round((closed - created) / (1000 * 60 * 60 * 24) * 10) / 10;

            const projectQuery = `
              query {
                organization(login: "footballguysdotcom") {
                  projectV2(number: 5) {
                    id
                    fields(first: 30) {
                      nodes {
                        ... on ProjectV2Field {
                          id
                          name
                          dataType
                        }
                      }
                    }
                  }
                }
              }
            `;
            const projectData = await github.graphql(projectQuery);
            const project = projectData.organization.projectV2;
            const timeToResolveField = project.fields.nodes.find(
              f => f.name === 'Time to resolve (days)' && (f.dataType === 'NUMBER' || !f.dataType)
            );
            if (!timeToResolveField) {
              console.log('Project has no Number field "Time to resolve (days)" – skip. Add it in project settings to enable.');
              return;
            }

            const itemQuery = `
              query($issueId: ID!) {
                node(id: $issueId) {
                  ... on Issue {
                    projectItems(first: 10) {
                      nodes {
                        id
                        project {
                          ... on ProjectV2 {
                            id
                          }
                        }
                      }
                    }
                  }
                }
              }
            `;
            const itemData = await github.graphql(itemQuery, { issueId: issue.node_id });
            const projectItems = itemData?.node?.projectItems?.nodes ?? [];
            const projectItem = projectItems.find(n => n.project?.id === project.id);
            if (!projectItem) {
              console.log('Issue is not on the Escalations project – skip.');
              return;
            }

            const updateMutation = `
              mutation($projectId: ID!, $itemId: ID!, $fieldId: ID!, $value: ProjectV2FieldValue!) {
                updateProjectV2ItemFieldValue(input: {
                  projectId: $projectId
                  itemId: $itemId
                  fieldId: $fieldId
                  value: $value
                }) {
                  projectV2Item { id }
                }
              }
            `;
            await github.graphql(updateMutation, {
              projectId: project.id,
              itemId: projectItem.id,
              fieldId: timeToResolveField.id,
              value: { number: days }
            });
            console.log('Set Time to resolve (days) to', days);
